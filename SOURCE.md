# Corresponding source

This static Chat NoControl build is licensed under AGPL-3.0-or-later.

The corresponding source is published at
<https://github.com/levi-cm/chat-nocontrol>. Use the signed release tag and
commit recorded in `release-record.json` to identify the exact build source.

Build and verification instructions are in `README.md` and
`docs/testing-and-release.md` in that source tree.
